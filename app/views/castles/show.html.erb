<div class="container mb-3 mt-3">
  <h2><strong><%= @castle.name %></strong></h2>

  <% if @castle.photo.present? %>
    <%= cl_image_tag @castle.photo.key, width: 400, height: 300, crop: :fill %>
  <% else %>
    <img width=400px height=300px src="https://www.distefanosales.com/wp-content/uploads/2023/08/image-coming-soon-placeholder.png" alt="">
  <% end %>
  <div class="mt-3">
    <p><%= @castle.description %></p>
  </div>

  <%= link_to "Retour à la liste", castles_path, class: "btn btn-primary" %>
</div>
